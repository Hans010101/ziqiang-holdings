const app = document.querySelector('#app');
const money = (n) => n >= 1e12 ? `$${(n/1e12).toFixed(2)}万亿` : n >= 1e8 ? `$${(n/1e8).toFixed(2)}亿` : n >= 1e4 ? `$${(n/1e4).toFixed(1)}万` : `$${Number(n||0).toLocaleString()}`;
const number = (n) => Number(n||0).toLocaleString('zh-CN',{maximumFractionDigits:2});
const pct = (n) => `${Number(n||0).toFixed(2)}%`;
const esc = (v) => String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const api = async (path) => { const r=await fetch(path); if(!r.ok) throw new Error((await r.json()).error||`请求失败 ${r.status}`); return r.json(); };
const label = {new:'新出现',increase:'增持披露',decrease:'减持披露',sold:'不再披露',unchanged:'未变'};
const source = (v) => v==='ARK'?'木头姐基金每日持仓':'美国证监会季度披露';
const managerNames = {
  hh:'段永平关联机构',berkshire:'巴菲特旗下伯克希尔',bridgewater:'桥水基金','pershing-square':'比尔·阿克曼关联机构',appaloosa:'阿帕卢萨',baupost:'包普斯特','third-point':'第三点',soros:'索罗斯基金',scion:'迈克尔·伯里旗下赛恩',greenlight:'大卫·艾因霍恩关联机构',icahn:'卡尔·伊坎','tiger-global':'老虎环球','lone-pine':'孤松资本',viking:'维京全球',altimeter:'高度计资本',himalaya:'李录旗下喜马拉雅',elliott:'埃利奥特',vanguard:'先锋领航','state-street':'道富',fidelity:'富达','t-rowe-price':'普信',jpmorgan:'摩根大通',blackrock:'贝莱德','goldman-sachs':'高盛','morgan-stanley':'摩根士丹利','bank-of-america':'美国银行',ubs:'瑞银',citadel:'城堡投资',millennium:'千禧管理',point72:'第七十二点',renaissance:'文艺复兴科技','de-shaw':'德劭',arkk:'木头姐创新主题基金',arkg:'木头姐基因主题基金',arkq:'木头姐自动化与机器人基金',arkw:'木头姐下一代互联网基金',arkf:'木头姐区块链与金融科技基金',arkx:'木头姐太空与国防创新基金'
};
const managerName = (m) => managerNames[m?.id] || m?.display_name || '未知机构';
const securityName = (s) => s?.name_cn ? `${s.name_cn}${s.ticker ? `（${s.ticker}）` : ''}` : s?.ticker || s?.issuer || '未知证券';
const syncState = (v) => ({completed:'已完成',success:'同步成功',partial:'部分成功',running:'同步中',failed:'同步失败'}[v] || v || '未知');

function set(html){app.innerHTML=html;window.scrollTo({top:0,behavior:'auto'});app.focus({preventScroll:true});}
function fail(err){set(`<div class="error"><span class="error-code">数据连接中断</span><h2>数据暂时无法读取</h2><p>${esc(err.message)}</p><button class="button" data-retry>重新连接</button></div>`);}
function table(rows,cols,empty='暂无数据'){
  if(!rows.length)return `<div class="empty">${empty}</div>`;
  return `<div class="table-wrap"><table><thead><tr>${cols.map(c=>`<th class="${c.num?'num':''}">${c.name}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${cols.map(c=>`<td class="${c.num?'num':''}">${c.render?c.render(r):esc(r[c.key])}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;
}
const managerCols=[
  {name:'投资人与机构',render:r=>`<a class="manager-link" href="#/manager/${esc(r.id)}">${esc(managerName(r))}</a><div class="source">${esc(r.category)} · ${source(r.source)}</div>`},
  {name:'报告期',key:'report_date'},{name:'披露日',key:'filed_date'},
  {name:'组合规模',num:1,render:r=>money(r.total_value)},{name:'持仓',num:1,render:r=>number(r.positions_count)}
];
const currentRows = (d) => d.positions.filter(x=>x.change_type!=='sold');
const ordinaryRows = (d) => currentRows(d).filter(x=>x.amount_type==='SH'&&!x.put_call);
const concentration = (d) => ordinaryRows(d).slice(0,10).reduce((a,x)=>a+Number(x.weight),0);
const changeRate = (r) => r.previous_shares ? (r.shares-r.previous_shares)/Math.abs(r.previous_shares)*100 : r.change_type==='new' ? 100 : r.change_type==='sold' ? -100 : 0;
const securityType = (r) => r.put_call ? `${String(r.put_call).toUpperCase()==='PUT'?'看跌':'看涨'}期权` : r.amount_type==='PRN' ? '本金金额（PRN）' : '普通股（SH）';
const initials = (name) => name.replace(/\s*\/.*$/,'').slice(0,2).toUpperCase();
const generatedPortraitIds = new Set(['icahn','scion','himalaya','altimeter','bridgewater','pershing-square','appaloosa','baupost','third-point','soros','greenlight','tiger-global','lone-pine','viking','elliott','citadel','millennium','point72','renaissance','de-shaw','bank-of-america','blackrock','fidelity','goldman-sachs','jpmorgan','morgan-stanley','state-street','t-rowe-price','ubs','vanguard']);
const institutionPortraitIds = new Set(['bank-of-america','blackrock','fidelity','goldman-sachs','jpmorgan','morgan-stanley','state-street','t-rowe-price','ubs','vanguard']);

function portrait(id,name){
  const institution=institutionPortraitIds.has(id),file=id==='hh'?'duan-yongping.png':id==='berkshire'?'warren-buffett.png':id.startsWith('ark')?'cathie-wood.png':generatedPortraitIds.has(id)?`${id}.jpg`:'';
  return file?`<img class="portrait-image${institution?' institution-image':''}" src="/assets/${file}" alt="${esc(name)}${institution?'机构视觉':'人物肖像'}">`:`<div class="portrait-fallback" aria-hidden="true">${esc(initials(name))}</div>`;
}

function donut(rows){
  const top=rows.slice(0,6),rest=Math.max(0,100-top.reduce((s,r)=>s+Number(r.weight),0));let offset=0;
  const circles=[...top.map((r,i)=>({value:Number(r.weight),tone:i})),{value:rest,tone:6}].map(x=>{const out=`<circle class="tone-${x.tone}" cx="64" cy="64" r="47" pathLength="100" stroke-dasharray="${x.value} ${100-x.value}" stroke-dashoffset="${-offset}"/>`;offset+=x.value;return out;}).join('');
  return `<div class="donut-wrap"><svg class="donut" viewBox="0 0 128 128" aria-label="前六大持仓权重">${circles}<circle class="donut-hole" cx="64" cy="64" r="35"/><text x="64" y="60">前六大</text><text class="donut-value" x="64" y="77">${pct(100-rest)}</text></svg><div class="legend">${top.map((r,i)=>`<a href="#/stock/${esc(r.cusip)}"><i class="tone-bg-${i}"></i><span>${esc(securityName(r))}</span><b>${pct(r.weight)}</b></a>`).join('')}</div></div>`;
}

function movementChart(rows){
  const movers=rows.filter(r=>r.amount_type==='SH'&&!r.put_call&&r.change_type!=='unchanged').sort((a,b)=>Math.abs(changeRate(b))-Math.abs(changeRate(a))).slice(0,7);
  if(!movers.length)return '<div class="empty compact">相邻报告期披露股数未发生变化</div>';
  const max=Math.max(...movers.map(r=>Math.abs(changeRate(r))),1);
  return `<div class="movement-chart">${movers.map(r=>{const rate=changeRate(r),width=Math.max(5,Math.abs(rate)/max*46),x=rate<0?50-width:50;return `<a href="#/stock/${esc(r.cusip)}" class="movement-row"><span>${esc(securityName(r))}</span><svg viewBox="0 0 100 12" preserveAspectRatio="none" aria-hidden="true"><line x1="50" y1="0" x2="50" y2="12"/><rect class="${rate>=0?'positive':'negative'}" x="${x}" y="2" width="${width}" height="8" rx="1"/></svg><b class="${rate>=0?'up':'down'}">${rate>0?'+':''}${rate.toFixed(1)}%</b></a>`;}).join('')}<div class="axis"><span>减少</span><span>0</span><span>增加</span></div></div>`;
}

async function home(){
  const [data,berkshire,duan,ark,consensus]=await Promise.all([api('/api/summary'),api('/api/managers/berkshire'),api('/api/managers/hh'),api('/api/managers/arkk'),api('/api/stocks')]);
  const featured=berkshire,rows=ordinaryRows(featured),news=featured.positions.filter(x=>x.amount_type==='SH'&&!x.put_call&&x.change_type==='new').length,sells=featured.positions.filter(x=>x.amount_type==='SH'&&!x.put_call&&x.change_type==='sold').length,s=data.stats;
  const people=[
    {id:'hh',name:'段永平',note:'长期价值投资',image:'duan-yongping.png',data:duan},
    {id:'berkshire',name:'巴菲特',note:'集中持有优质企业',image:'warren-buffett.png',data:berkshire},
    {id:'arkk',name:'木头姐',note:'关注颠覆式创新',image:'cathie-wood.png',data:ark}
  ];
  set(`<section class="home-hero">
    <div class="hero-copy"><div class="eyebrow"><i></i>公开持仓研究平台</div><h1>从公开披露中，<br><em>看见高手的选择</em></h1><p>追踪知名投资人和机构的持仓变化。以披露股数为准，不把市值涨跌误写成买卖。</p><div class="hero-actions"><a class="button" href="#/managers">查看投资机构 <span>→</span></a><a class="text-link" href="#/sources">了解数据口径</a></div></div>
    <aside class="radar-card"><div class="radar-rings" aria-hidden="true"><i></i><i></i><i></i><b></b></div><div class="radar-copy"><span>全站覆盖</span><strong>${number(s.managers)} 家</strong><p>知名投资人、主动基金、量化机构与大型资管机构</p></div><div class="radar-meta"><span>已接通 <b>${number(s.synced)} 家</b></span><span>持仓记录 <b>${number(s.positions)} 条</b></span></div></aside>
  </section>
  <section class="people-section"><div class="section-head"><div><div class="eyebrow">重点关注</div><h2>从熟悉的投资人开始</h2><p>每位投资人进入独立详情页，不在当前列表内切换内容。</p></div><a href="#/managers">查看全部机构 →</a></div><div class="people-grid">${people.map(p=>`<a class="person-card" href="#/manager/${p.id}"><div class="person-image"><img src="/assets/${p.image}" alt="${p.name}人物肖像"></div><div class="person-info"><span>${p.note}</span><h3>${p.name}</h3><p>${p.data.current?`${esc(p.data.current.report_date)} · ${money(p.data.current.total_value)}`:'数据同步中'}</p><b>查看持仓详情 →</b></div></a>`).join('')}</div></section>
  <div class="ticker-strip"><span><i></i> 数据已接通 ${number(s.synced)}/${number(s.managers)} 家</span><span>公开组合规模 ${money(s.total_value)}</span><span>持仓记录 ${number(s.positions)} 条</span><span>最近同步 ${esc(data.sync?.finished_at||'进行中')}</span></div>
  <section class="section" id="featured"><div class="section-head"><div><div class="eyebrow">本期聚焦</div><h2>巴菲特最新持仓概览</h2></div><a href="#/manager/berkshire">进入独立详情页 →</a></div>
    <div class="kpi-grid"><div class="kpi featured"><span>前十大集中度</span><strong>${pct(concentration(featured))}</strong><small>按本期披露市值计算</small></div><div class="kpi"><span>披露项目</span><strong>${number(featured.current?.positions_count)}</strong><small>${esc(featured.current?.report_date||'等待校验')}</small></div><div class="kpi positive"><span>新出现</span><strong>${news}</strong><small>普通股相邻报告期对比</small></div><div class="kpi negative"><span>不再披露</span><strong>${sells}</strong><small>不等同实时卖出</small></div></div>
    <div class="cockpit-grid"><article class="panel"><div class="panel-head"><div><span>组合结构</span><h3>核心持仓分布</h3></div><b>${rows.length} 项持仓</b></div>${donut(rows)}</article><article class="panel"><div class="panel-head"><div><span>披露股数变化</span><h3>季度持股变化</h3></div><b>对比 ${esc(featured.previous?.report_date||'—')}</b></div>${movementChart(featured.positions)}</article></div>
  </section>
  <section class="section dual-section"><article><div class="section-head"><div><div class="eyebrow">机构共识</div><h2>被更多机构共同持有</h2></div><a href="#/stocks">查看全部证券 →</a></div><div class="consensus-list">${consensus.slice(0,6).map((r,i)=>`<a href="#/stock/${esc(r.cusip)}"><em>${String(i+1).padStart(2,'0')}</em><div><strong>${esc(securityName(r))}</strong><span>${esc(r.issuer)}</span></div><b>${r.managers} 家</b></a>`).join('')}</div></article><article><div class="section-head"><div><div class="eyebrow">最新动态</div><h2>最近公开披露</h2></div><a href="#/managers">查看全部机构 →</a></div><div class="filing-feed">${data.latest.slice(0,6).map(r=>`<a href="#/manager/${esc(r.id)}"><i></i><div><strong>${esc(managerName(r))}</strong><span>${esc(r.filed_date)} · ${source(r.source)}</span></div><b>${money(r.total_value)}</b></a>`).join('')}</div></article></section>`);
}

async function managersPage(){
  const rows=await api('/api/managers');
  set(`<div class="page-head"><div><div class="eyebrow">投资机构库</div><h1>选择一位投资人或机构</h1><p>${rows.length} 家公开披露主体。点击任一机构，查看完整持仓、季度变化与历史披露。</p></div><div class="page-counter"><b>${rows.length}</b><span>家机构</span></div></div><div class="toolbar"><div class="tabs" id="category-tabs"><button class="active" data-category="">全部</button>${[...new Set(rows.map(r=>r.category))].map(x=>`<button data-category="${esc(x)}">${esc(x)}</button>`).join('')}</div><input id="manager-search" type="search" placeholder="搜索投资人或机构" aria-label="搜索机构"></div><div id="manager-list">${managerCards(rows)}</div>`);
  const render=()=>{const q=document.querySelector('#manager-search').value.toLowerCase(),cat=document.querySelector('#category-tabs .active').dataset.category;document.querySelector('#manager-list').innerHTML=managerCards(rows.filter(r=>(!cat||r.category===cat)&&`${managerName(r)} ${r.display_name} ${r.name}`.toLowerCase().includes(q)));};
  document.querySelector('#manager-search').addEventListener('input',render);document.querySelector('#category-tabs').addEventListener('click',e=>{if(e.target.matches('button')){document.querySelectorAll('#category-tabs button').forEach(x=>x.classList.remove('active'));e.target.classList.add('active');render();}});
}
function managerCards(rows){return rows.length?`<div class="manager-list">${rows.map((r,i)=>{const stale=Number(r.is_stale)===1,status=r.notice_form&&stale?'当前为 13F-NT':stale?'仅有历史披露':'当前申报已校验',prefix=stale?'历史':'';return `<a class="manager-row" href="#/manager/${esc(r.id)}" aria-label="查看${esc(managerName(r))}的持仓详情"><em>${String(i+1).padStart(2,'0')}</em><div class="manager-avatar">${portrait(r.id,managerName(r))}</div><div class="manager-name"><span class="badge">${esc(r.category)}</span><h3>${esc(managerName(r))}</h3><small>${esc(status)}<br>${r.report_date?`报告期 ${esc(r.report_date)} · 披露 ${esc(r.filed_date)}`:'等待官方数据校验'}</small></div><div class="manager-holdings"><span>${prefix}前三大普通股</span><strong>${[r.top_holding_1,r.top_holding_2,r.top_holding_3].filter(Boolean).map(esc).join(' · ')||'暂无可用数据'}</strong></div><div class="manager-metrics"><span><small>${prefix}组合规模</small><strong>${r.total_value?money(r.total_value):'—'}</strong></span><span><small>${prefix}披露项目</small><strong>${r.positions_count==null?'—':number(r.positions_count)}</strong></span><span><small>${prefix}前十集中度</small><strong>${r.top10_weight==null?'—':pct(r.top10_weight)}</strong></span></div><span class="manager-arrow" aria-hidden="true">↗</span></a>`}).join('')}</div>`:'<div class="empty">没有匹配的机构</div>';}

async function managerPage(id){
  const d=await api(`/api/managers/${encodeURIComponent(id)}`),m=d.manager;
  const display=managerName(m);
  if(!d.current){const notice=d.notices?.[0],history=d.historical,others=notice?JSON.parse(notice.other_managers||'[]'):[];set(`<nav class="breadcrumb" aria-label="当前位置"><a href="#/managers">投资机构</a><span>›</span><strong>${esc(display)}</strong></nav><div class="page-head"><div><div class="eyebrow">${source(m.source)}</div><h1>${esc(display)}</h1><p>${esc(m.data_note||'当前没有通过时效与完整性校验的公开持仓。')}</p></div></div><div class="notice"><b>${notice?'13F-NT 申报通知':'当前持仓未纳入'}</b> ${esc(notice?.detail||'仅保留历史披露供溯源，不进入首页、机构共识或对比。')}${notice?` <a href="${esc(notice.source_url)}" target="_blank" rel="noreferrer">查看官方原文 ↗</a>`:''}</div>${others.length?`<section class="section"><div class="section-head"><div><div class="eyebrow">申报去向</div><h2>通知列出的关联申报主体</h2></div></div>${table(others,[{name:'官方名称',key:'name'},{name:'美国证监会主体编号',render:r=>esc(r.cik)}])}</section>`:''}${history?`<section class="section"><div class="section-head"><div><div class="eyebrow">历史留档</div><h2>最近一份已校验历史申报</h2></div></div><div class="notice">报告期 ${esc(history.report_date)} · 披露 ${esc(history.filed_date)} · ${money(history.total_value)}。此数据不代表当前仓位。 <a href="${esc(history.source_url)}" target="_blank" rel="noreferrer">查看历史原文 ↗</a></div></section>`:'<div class="empty">等待权威公开源完成校验。</div>'}`);return;}
  const top=ordinaryRows(d),newCount=d.positions.filter(x=>x.amount_type==='SH'&&!x.put_call&&x.change_type==='new').length,soldCount=d.positions.filter(x=>x.amount_type==='SH'&&!x.put_call&&x.change_type==='sold').length,max=Math.max(...d.filings.map(x=>Number(x.total_value)),1);
  set(`<nav class="breadcrumb" aria-label="当前位置"><a href="#/managers">投资机构</a><span>›</span><strong>${esc(display)}</strong></nav><section class="manager-hero"><div class="manager-portrait">${portrait(id,display)}</div><div class="manager-intro"><div class="eyebrow">${source(m.source)} · ${esc(m.category)}</div><h1>${esc(display)}</h1><p>公开持仓独立详情页</p><div class="manager-tags"><span>报告期 ${esc(d.current.report_date)}</span><span>披露日期 ${esc(d.current.filed_date)}</span><a href="${esc(d.current.source_url)}" target="_blank" rel="noreferrer">查看原始披露 ↗</a></div></div><div class="manager-hero-value"><span>披露组合市值</span><strong>${money(d.current.total_value)}</strong><small>${number(d.current.positions_count)} 项持仓</small></div></section>
  <nav class="detail-tabs" aria-label="详情页目录"><a href="#manager-overview">组合概览</a><a href="#manager-positions">持仓明细</a><a href="#manager-history">历史披露</a><a href="#/managers">返回机构列表</a></nav>
  <div class="kpi-grid manager-kpis"><div class="kpi featured"><span>前十大集中度</span><strong>${pct(concentration(d))}</strong><small>按披露市值计算</small></div><div class="kpi positive"><span>新出现</span><strong>${newCount}</strong><small>相邻报告期</small></div><div class="kpi negative"><span>不再披露</span><strong>${soldCount}</strong><small>不等同实时卖出</small></div><div class="kpi"><span>数据口径</span><strong>股数</strong><small>排除市值涨跌干扰</small></div></div>
  <div class="notice"><b>校验状态</b> ${esc(d.current.validation_detail)}${m.data_note?`<br>${esc(m.data_note)}`:''}<br><b>口径说明</b> 普通股、期权与本金金额分开显示；变化依据相邻报告期同一证券类型的披露数量计算。拆股、合并、换股和代码变更可能造成非交易变化，因此统一称“披露持股变化”。</div>
  <section class="section cockpit-grid" id="manager-overview"><article class="panel"><div class="panel-head"><div><span>组合结构</span><h3>核心持仓</h3></div><b>${esc(d.current.report_date)}</b></div>${donut(top)}</article><article class="panel"><div class="panel-head"><div><span>季度变化</span><h3>持股变化对比</h3></div><b>对比 ${esc(d.previous?.report_date||'—')}</b></div>${movementChart(d.positions)}</article></section>
  <section class="section" id="manager-positions"><div class="section-head"><div><div class="eyebrow">持仓明细</div><h2>完整持仓与变化</h2></div><div class="section-tools"><select id="change-filter" aria-label="筛选变化"><option value="">全部变化</option>${Object.entries(label).map(([k,v])=>`<option value="${k}">${v}</option>`).join('')}</select><a class="button secondary" href="/api/export.csv?manager=${encodeURIComponent(id)}">导出表格</a></div></div><div id="positions">${positionsTable(d.positions)}</div></section>
  <section class="section" id="manager-history"><div class="section-head"><div><div class="eyebrow">历史披露</div><h2>组合规模变化</h2></div></div><div class="panel timeline">${d.filings.filter((x,i,a)=>i===a.findIndex(y=>y.report_date===x.report_date)).map(f=>`<div class="bar-row"><span>${esc(f.report_date)}</span><progress value="${Number(f.total_value)}" max="${max}"></progress><strong>${money(f.total_value)}</strong></div>`).join('')}</div></section>`);
  document.querySelector('#change-filter').addEventListener('change',e=>document.querySelector('#positions').innerHTML=positionsTable(d.positions.filter(x=>!e.target.value||x.change_type===e.target.value)));
}
function positionsTable(rows){return table(rows,[
  {name:'证券',render:r=>`${r.amount_type==='SH'&&!r.put_call?`<a class="security" href="#/stock/${esc(r.cusip)}">${esc(securityName(r))}</a>`:`<strong>${esc(securityName(r))}</strong>`}<div class="source">${esc(r.issuer)} · ${esc(r.cusip)}</div>`},
  {name:'证券类型',render:r=>`<span class="sector">${esc(securityType(r))}</span>`},
  {name:'行业',render:r=>r.sector?`<span class="sector">${esc(r.sector)}</span>`:'—'},
  {name:'披露变化',render:r=>`<span class="change ${esc(r.change_type)}">${label[r.change_type]}</span>`},
  {name:'本期披露数量',num:1,render:r=>number(r.shares)},{name:'上期披露数量',num:1,render:r=>r.previous_shares==null?'—':number(r.previous_shares)},
  {name:'数量变化',num:1,render:r=>r.previous_shares?`${changeRate(r)>0?'+':''}${changeRate(r).toFixed(1)}%`:'—'},
  {name:'市值',num:1,render:r=>money(r.value)},{name:'权重',num:1,render:r=>pct(r.weight)}
]);}

async function stocksPage(){
  const [initial,sectors]=await Promise.all([api('/api/stocks'),api('/api/sectors')]);
  set(`<div class="page-head"><div><div class="eyebrow">持仓证券库</div><h1>查看机构共同持有的证券</h1><p>只聚合通过时效校验的普通股多头（SH 且非期权）；不同机构的报告期可能不同。</p></div></div><form id="stock-form" class="search-console"><input id="stock-q" type="search" placeholder="输入中文名称、交易代码或证券识别码" aria-label="搜索证券"><select id="sector-filter" aria-label="筛选板块"><option value="">全部板块</option>${sectors.map(x=>`<option>${esc(x.sector)}</option>`).join('')}</select><button class="button">搜索</button></form><div id="stock-results">${stocksTable(initial)}</div>`);
  document.querySelector('#stock-form').addEventListener('submit',async e=>{e.preventDefault();const q=document.querySelector('#stock-q').value,sector=document.querySelector('#sector-filter').value;document.querySelector('#stock-results').innerHTML='<div class="loading">正在聚合机构持仓…</div>';document.querySelector('#stock-results').innerHTML=stocksTable(await api(`/api/stocks?q=${encodeURIComponent(q)}&sector=${encodeURIComponent(sector)}`));});
}
function stocksTable(rows){return table(rows,[{name:'证券',render:r=>`<a class="security-name" href="#/stock/${esc(r.cusip)}">${esc(securityName(r))}</a><div class="source">${esc(r.issuer)} · ${esc(r.cusip)}</div>`},{name:'板块与行业',render:r=>r.sector?`<span class="sector">${esc(r.sector)}</span><div class="source industry">${esc(r.industry)}</div>`:'—'},{name:'持有机构',num:1,render:r=>`${number(r.managers)} 家`},{name:'合计披露市值',num:1,render:r=>money(r.total_value)},{name:'合计股数',num:1,render:r=>number(r.total_shares)}]);}
async function stockPage(cusip){const d=await api(`/api/stocks/${encodeURIComponent(cusip)}`),s=d.security;set(`<nav class="breadcrumb" aria-label="当前位置"><a href="#/stocks">持仓证券</a><span>›</span><strong>${esc(securityName(s))}</strong></nav><div class="security-hero"><div><div class="eyebrow">证券识别码 ${esc(s.cusip)}</div><h1>${esc(securityName(s))}</h1><p>${esc(s.issuer)} · ${d.holders.length} 家追踪机构最新披露</p></div><div class="security-meta"><span>${esc(s.sector||'板块待匹配')}</span><b>${esc(s.industry||'公开目录暂无可靠行业')}</b></div></div><div class="section-head compact-head"><div><div class="eyebrow">持有机构</div><h2>哪些机构持有这只证券</h2></div><a href="#/stocks">返回证券列表 →</a></div>${table(d.holders,[{name:'机构',render:r=>`<strong>${esc(managerName({id:r.manager_id,display_name:r.display_name}))}</strong><div class="source">${esc(r.category)}</div>`},{name:'报告期',key:'report_date'},{name:'股数',num:1,render:r=>number(r.shares)},{name:'市值',num:1,render:r=>money(r.value)},{name:'组合权重',num:1,render:r=>pct(r.weight)},{name:'操作',render:r=>`<a class="table-action" href="#/manager/${esc(r.manager_id)}">查看机构 →</a>`}])}`);}

async function comparePage(){
  const ms=await api('/api/managers'),options=ms.map(m=>`<option value="${esc(m.id)}">${esc(managerName(m))}</option>`).join('');
  set(`<div class="page-head"><div><div class="eyebrow">机构对比</div><h1>把机构放到同一张表里</h1><p>选择 2–5 家机构，比较最新报告期规模、集中度与重合持仓。</p></div></div><form id="compare-form" class="panel compare-panel"><div class="compare-picks">${[0,1,2,3,4].map((_,i)=>`<label><span>机构 ${i+1}</span><select name="manager"><option value="">${i>1?'可选':'请选择'}</option>${options}</select></label>`).join('')}</div><button class="button">生成对比</button></form><div id="compare-result"></div>`);
  document.querySelector('#compare-form').addEventListener('submit',async e=>{e.preventDefault();const ids=[...new FormData(e.target).getAll('manager')].filter((x,i,a)=>x&&a.indexOf(x)===i);if(ids.length<2){document.querySelector('#compare-result').innerHTML='<div class="error compact">请选择至少两家不同机构</div>';return;}document.querySelector('#compare-result').innerHTML='<div class="loading">正在比对持仓…</div>';renderCompare(await api(`/api/compare?ids=${ids.join(',')}`),ids,ms);});
}
function renderCompare(rows,ids,ms){
  const group=(items,key)=>items.reduce((all,item)=>((all[item[key]]??=[]).push(item),all),{}),by=group(rows,'id'),summary=ids.map(id=>{const holdings=by[id]||[],meta=holdings[0]||ms.find(x=>x.id===id);return {...meta,top10:holdings.slice(0,10).reduce((a,x)=>a+Number(x.weight),0)}}),overlap=Object.values(group(rows,'cusip')).filter(g=>new Set(g.map(x=>x.id)).size>1).sort((a,b)=>b.length-a.length||b.reduce((s,x)=>s+x.value,0)-a.reduce((s,x)=>s+x.value,0));
  document.querySelector('#compare-result').innerHTML=`<div class="compare-summary">${summary.map(x=>`<article><span>${esc(managerName(x))}</span><strong>${money(x.total_value)}</strong><small>${esc(x.report_date||'无数据')} · 前十 ${pct(x.top10)}</small></article>`).join('')}</div><section class="section"><div class="section-head"><h2>重合持仓</h2></div>${table(overlap,[{name:'证券',render:g=>`<a class="security" href="#/stock/${esc(g[0].cusip)}">${esc(securityName(g[0]))}</a>`},{name:'共同持有',render:g=>g.map(x=>esc(managerName(x))).join('、')},{name:'合计市值',num:1,render:g=>money(g.reduce((s,x)=>s+x.value,0))}],'未发现重合持仓')}</section>`;
}

async function alertsPage(){const d=await api('/api/summary');set(`<div class="page-head"><div><div class="eyebrow">变化提醒</div><h1>及时收到新的公开披露</h1><p>订阅最新申报、新出现、不再披露与显著股数变化。</p></div></div><div class="alert-grid"><article class="panel alert-card"><span class="alert-icon">订阅</span><div><h3>订阅披露变化</h3><p>可添加到常见的资讯订阅工具。选择披露股数变化阈值：</p><div class="alert-actions"><a class="button secondary" href="/api/feed.xml?threshold=10">百分之十</a><a class="button" href="/api/feed.xml?threshold=20">百分之二十</a><a class="button secondary" href="/api/feed.xml?threshold=50">百分之五十</a></div></div></article><article class="panel alert-card"><span class="alert-icon pulse">↻</span><div><h3>每小时自动检查</h3><p>源站未更新时不会生成重复记录。提醒描述的是公开披露变化，不等同实时交易。</p><div class="sync-status"><i></i>${esc(syncState(d.sync?.status))} · ${esc(d.sync?.finished_at||'同步中')}</div></div></article></div><section class="section"><div class="section-head"><div><div class="eyebrow">最近披露</div><h2>最新机构申报</h2></div></div>${table(d.latest,managerCols)}</section>`);}

async function sourcesPage(){const d=await api('/api/sources');set(`<div class="page-head"><div><div class="eyebrow">数据来源</div><h1>每项数据都能回到权威公开源</h1><p>原始监管披露优先；每份申报先对账，再判断能否进入当前组合。</p></div><div class="sync-badge"><i></i>${esc(syncState(d.sync?.status))}</div></div><div class="source-stack">${d.sources.map((s,i)=>`<article><em>${String(i+1).padStart(2,'0')}</em><div><span>${esc(s.cadence)}</span><h2>${esc(s.name)}</h2><p>${esc(s.detail)}</p><a href="${esc(s.official_url)}" target="_blank" rel="noreferrer">访问官方来源 ↗</a></div><div class="source-count">${s.status?`<strong>${esc(s.status)}</strong><span>当前处理状态</span>`:s.managers!=null?`<strong>${number(s.current)}/${number(s.managers)}</strong><span>当前申报可用</span><small>${s.stale?`${number(s.stale)} 家仅有历史申报 · `:''}已校验 ${number(s.synced)} 家 · ${esc(s.updated_at||'等待更新')}</small>`:`<strong>${number(s.securities)}</strong><span>${esc(s.count_label||'记录已核验')}</span><small>${esc(s.updated_at||'等待更新')}</small>`}</div></article>`).join('')}</div><div class="data-boundary"><div class="eyebrow">使用前须知</div><h2>公开披露的边界</h2><div><p><b>人物页展示的是关联申报机构。</b>它不是个人全部资产，也不代表其未披露账户。</p><p><b>13F 只覆盖特定美国多头证券。</b>不覆盖现金、空头、私募资产及大量境外本地证券；普通股、期权和本金金额分别展示。</p><p><b>季度披露有滞后。</b>季末后最长 45 天才披露；13F-NT 没有持仓表，历史组合不会被当作当前仓位。</p><p><b>中文名称与行业用于辅助阅读。</b>身份仍以原始申报名、证券识别码和原文链接为准。</p><p><b>权威来源也可能含申报错误。</b>封面与信息表不一致会阻断入库，异常金额单位会保留原值并明确标记推断。</p><p><b>披露数量变化不必然是交易。</b>拆股、合并、换股与代码变更均可能造成变化。</p><p><b>香港交易所数据当前未接入。</b>未获系统化提取与再发布授权前，不展示抓取记录；媒体报道只作为线索，不能替代法定持股数量。</p></div></div>`);}

const routes={managers:managersPage,stocks:stocksPage,compare:comparePage,alerts:alertsPage,sources:sourcesPage};
async function route(){try{window.scrollTo({top:0,behavior:'auto'});app.innerHTML='<div class="loading">正在读取公开披露数据…</div>';const parts=location.hash.replace(/^#\/?/,'').split('/').filter(Boolean),name=parts[0],navName=name==='manager'?'managers':name==='stock'?'stocks':name;document.querySelectorAll('.topbar nav a').forEach(a=>a.classList.toggle('active',a.hash===`#/${navName||''}`));if(name==='manager')await managerPage(parts[1]);else if(name==='stock')await stockPage(parts[1]);else await (routes[name]||home)();}catch(err){fail(err);}}
document.querySelector('.nav-toggle').addEventListener('click',e=>{const nav=document.querySelector('nav'),open=nav.classList.toggle('open');e.target.setAttribute('aria-expanded',open)});document.querySelector('nav').addEventListener('click',()=>document.querySelector('nav').classList.remove('open'));app.addEventListener('click',e=>{if(e.target.matches('[data-retry]'))route();});addEventListener('hashchange',route);route();
